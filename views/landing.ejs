<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('./partials/head') %>
  <title>Welcome to BlogV4</title>
  <style>
    body {
      background-color: #0f0f0f;
      color: #f8f9fa;
      font-family: 'Segoe UI', sans-serif;
    }
    .hero-section {
      padding: 80px 0;
      text-align: center;
      background: linear-gradient(135deg, #2c003e, #0a0f2c);
    }
    .hero-title {
      font-size: 3rem;
      color: #f72585;
      font-weight: bold;
      opacity: 0;
      transform: translateY(30px);
    }
    .hero-subtitle {
      font-size: 1.25rem;
      margin: 20px 0;
      color: #ccc;
      opacity: 0;
      transform: translateY(30px);
    }
    .hero-buttons {
      opacity: 0;
      transform: translateY(30px);
    }
    .hero-buttons a {
      margin: 10px;
      padding: 12px 25px;
      font-size: 1rem;
      border-radius: 30px;
      text-decoration: none;
      transition: 0.3s ease;
    }
    .btn-pink {
      background-color: #f72585;
      color: white;
    }
    .btn-pink:hover {
      background-color: #d11e72;
    }
    .features {
      padding: 60px 0;
      text-align: center;
    }
    .features h2 {
      color: #f72585;
      margin-bottom: 40px;
    }
    .feature-card {
      background: #1a1a1a;
      border-radius: 12px;
      padding: 30px;
      margin: 10px;
      color: #eee;
      box-shadow: 0 0 15px rgba(255, 0, 255, 0.05);
      opacity: 0;
      transform: translateY(50px);
    }
  </style>
</head>
<body>

  <%- include('./partials/nav') %>

  <!-- Hero Section -->
  <section class="hero-section">
    <div class="container">
      <h1 class="hero-title">Welcome to BlogV4</h1>
      <p class="hero-subtitle">Share your voice. Read stories. Discover ideas in a funky neon way!</p>
      <div class="hero-buttons">
        <a href="/user/signup" class="btn btn-pink">Create Account</a>
        <a href="/user/signin" class="btn btn-outline-light">Sign In</a>
        <a href="/home" class="btn btn-outline-info">Browse Blogs</a>
      </div>
    </div>
  </section>

  <!-- Features -->
  <section class="features container">
    <h2>Why BlogV4?</h2>
    <div class="row row-cols-1 row-cols-md-3 g-4">
      <div class="col">
        <div class="feature-card"> <!-- card 1 -->
          <h5>📝 Write Blogs Easily</h5>
          <p>Simple, clean editor to help you express your thoughts without limits.</p>
        </div>
      </div>
      <div class="col">
        <div class="feature-card"> <!-- card 2 -->
          <h5>💬 Comment & Connect</h5>
          <p>Interact with other authors and build a mini community around your posts.</p>
        </div>
      </div>
      <div class="col">
        <div class="feature-card"> <!-- card 3 -->
          <h5>Modern UI</h5>
          <p>Enjoy a fresh neon-themed interface that makes reading fun again.</p>
        </div>
      </div>
    </div>
  </section>

  <%- include('./partials/scripts') %>

  <!-- GSAP CDN -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
  <script>
    // Animate hero elements
    gsap.to(".hero-title", {
      y: 0,
      opacity: 1,
      duration: 1,
      ease: "power3.out"
    });

    gsap.to(".hero-subtitle", {
      y: 0,
      opacity: 1,
      delay: 0.2,
      duration: 1,
      ease: "power3.out"
    });

    gsap.to(".hero-buttons", {
      y: 0,
      opacity: 1,
      delay: 0.4,
      duration: 1,
      ease: "power3.out"
    });

    // Animate feature cards with stagger
    gsap.to(".feature-card", {
      y: 0,
      opacity: 1,
      duration: 1,
      ease: "power3.out",
      stagger: 0.3,
      delay: 0.5
    });
  </script>
</body>
</html>
